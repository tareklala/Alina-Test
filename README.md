# MyCppProject

This is a ready-to-run C++ project for **GitHub Codespaces**.

## How to run

1. Open in GitHub Codespaces.
2. Terminal commands:

```bash
cmake -S . -B build
cmake --build build
./build/AlinaTest
